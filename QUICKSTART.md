# 🚀 Быстрый старт

## Шаг 1: Установка Python

Убедитесь что у вас установлен Python 3.12 или выше:

```powershell
python --version
```

Если Python не установлен, скачайте с [python.org](https://www.python.org/downloads/)

## Шаг 2: Установка зависимостей

### Вариант A: Автоматическая установка (рекомендуется)

```powershell
.\install.ps1
```

### Вариант B: Ручная установка

```powershell
# Установить зависимости
pip install -r requirements.txt

# Создать .env файл
copy .env.example .env
```

## Шаг 3: Создание Telegram бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username бота)
4. Получите токен (формат: `123456:ABC-DEF...`)
5. Скопируйте токен

## Шаг 4: Настройка бота

Отредактируйте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER
```

## Шаг 5: Настройка базы знаний (опционально)

Отредактируйте `data/faq.json` и добавьте информацию о ваших услугах и FAQ.

## Шаг 6: Установка Ollama (опционально, но рекомендуется)

### Для умных AI-ответов:

1. Скачайте Ollama с [ollama.ai](https://ollama.ai)
2. Установите
3. Откройте PowerShell и выполните:

```powershell
ollama pull llama3.2:3b
```

4. Запустите Ollama (обычно запускается автоматически)

### Без Ollama:

Бот будет работать в режиме простого поиска по FAQ (без AI генерации).

## Шаг 7: Запуск бота

### Вариант A: Через скрипт

```powershell
.\run.ps1
```

### Вариант B: Напрямую

```powershell
python src\main.py
```

## Шаг 8: Тестирование

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Попробуйте задать вопрос!

## Остановка бота

Нажмите `Ctrl+C` в консоли где запущен бот.

---

## Устранение проблем

### "TELEGRAM_BOT_TOKEN not found"

- Проверьте что файл `.env` существует
- Убедитесь что токен правильно вставлен (без пробелов)

### "Ollama недоступна"

- Это нормально, если Ollama не установлена
- Бот будет работать в fallback режиме (простой поиск)
- Для установки Ollama см. Шаг 6

### Бот не отвечает

- Проверьте что бот запущен (в консоли должны быть логи)
- Убедитесь что интернет работает
- Проверьте токен бота

---

## Что дальше?

- Настройте `data/faq.json` под ваш бизнес
- Добавьте больше вопросов-ответов
- Пригласите клиентов протестировать бота
- Изучите логи в `bot.log` для анализа вопросов

Полная документация в [README.md](README.md)
