# Руководство по конфигурации бота

## Обзор

Бот использует файл `config.json` для универсальных настроек компании и поведения бота. Это позволяет легко адаптировать бота для любого бизнеса без изменения кода.

## Структура config.json

### Секция company (обязательная)

Информация о компании:

```json
{
  "company": {
    "name": "Ваша Компания",
    "description": "Краткое описание компании",
    "phone": "+7 (999) 123-45-67",
    "email": "info@example.com",
    "telegram": "@manager_username",
    "website": "https://example.com"
  }
}
```

**Обязательные поля:**
- `name` - название компании
- `phone` - телефон для связи
- `email` - email для связи
- `telegram` - Telegram username менеджера

### Секция bot (обязательная)

Настройки поведения бота:

```json
{
  "bot": {
    "welcome_message": "Привет! Я AI-ассистент компании {company_name}...",
    "personality": "дружелюбный и профессиональный ассистент",
    "sales_strategy": "естественный диалог с постепенным склонением к действию",
    "language": "ru",
    "max_response_length": 300
  }
}
```

**Параметры:**
- `welcome_message` - приветственное сообщение (может использовать `{company_name}`)
- `personality` - описание характера бота
- `sales_strategy` - стратегия продаж
- `language` - язык бота (ru, en)
- `max_response_length` - максимальная длина ответа (в словах)

### Секция ai (обязательная)

Настройки AI модели:

```json
{
  "ai": {
    "model": "llama3.2:3b",
    "temperature": 0.5,
    "max_tokens": 300,
    "context_messages": 5
  }
}
```

**Параметры:**
- `model` - название модели Ollama
- `temperature` - температура генерации (0.0-1.0)
  - 0.0 = детерминированный, предсказуемый
  - 1.0 = креативный, разнообразный
- `max_tokens` - максимальное количество токенов в ответе
- `context_messages` - количество сообщений в истории диалога

### Секция features (обязательная)

Включение/выключение функций:

```json
{
  "features": {
    "quick_faq": true,
    "streaming": true,
    "progress_bar": true,
    "hints": false,
    "onboarding_tips": false
  }
}
```

**Параметры:**
- `quick_faq` - быстрая проверка FAQ перед AI
- `streaming` - потоковая генерация ответов
- `progress_bar` - анимированный индикатор прогресса
- `hints` - подсказки пользователю
- `onboarding_tips` - обучающие советы для новых пользователей

### Секция faq (обязательная)

Настройки поиска по FAQ:

```json
{
  "faq": {
    "quick_check_threshold": 0.75,
    "search_threshold": 0.4,
    "max_results": 3
  }
}
```

**Параметры:**
- `quick_check_threshold` - порог для быстрого FAQ (0.0-1.0)
  - Чем выше, тем точнее должно быть совпадение
- `search_threshold` - порог для поиска в FAQ (0.0-1.0)
- `max_results` - максимальное количество результатов FAQ

## Валидация конфигурации

При запуске бот автоматически валидирует конфигурацию:

1. **Проверка структуры** - все обязательные секции присутствуют
2. **Проверка типов** - значения соответствуют ожидаемым типам
3. **Проверка диапазонов** - числовые значения в допустимых границах

При ошибке валидации бот выдаст понятное сообщение об ошибке.

## Примеры конфигураций

### Минималистичный бот

```json
{
  "company": {
    "name": "Simple Company",
    "phone": "+7 (999) 111-22-33",
    "email": "info@simple.com",
    "telegram": "@simple_manager"
  },
  "bot": {
    "welcome_message": "Здравствуйте! Чем могу помочь?",
    "personality": "лаконичный помощник",
    "sales_strategy": "прямые ответы",
    "language": "ru",
    "max_response_length": 200
  },
  "ai": {
    "model": "llama3.2:3b",
    "temperature": 0.3,
    "max_tokens": 200,
    "context_messages": 3
  },
  "features": {
    "quick_faq": true,
    "streaming": false,
    "progress_bar": false,
    "hints": false,
    "onboarding_tips": false
  },
  "faq": {
    "quick_check_threshold": 0.8,
    "search_threshold": 0.5,
    "max_results": 2
  }
}
```

### Креативный бот с полным функционалом

```json
{
  "company": {
    "name": "Creative Agency",
    "description": "Мы создаем инновационные решения",
    "phone": "+7 (999) 777-88-99",
    "email": "hello@creative.agency",
    "telegram": "@creative_manager",
    "website": "https://creative.agency"
  },
  "bot": {
    "welcome_message": "Привет! 🎨 Я AI-ассистент {company_name}. Давайте создадим что-то удивительное вместе!",
    "personality": "креативный, энергичный, вдохновляющий ассистент",
    "sales_strategy": "эмоциональное вовлечение и вдохновение",
    "language": "ru",
    "max_response_length": 400
  },
  "ai": {
    "model": "llama3.2:3b",
    "temperature": 0.7,
    "max_tokens": 400,
    "context_messages": 7
  },
  "features": {
    "quick_faq": true,
    "streaming": true,
    "progress_bar": true,
    "hints": true,
    "onboarding_tips": true
  },
  "faq": {
    "quick_check_threshold": 0.7,
    "search_threshold": 0.3,
    "max_results": 5
  }
}
```

## Использование в коде

```python
from src.utils.config_loader import BotConfig

# Загрузить конфигурацию
config = BotConfig("config.json")

# Получить значения
company_name = config.company_name
temperature = config.ai_temperature
is_streaming_enabled = config.streaming_enabled

# Или использовать метод get() для вложенных значений
website = config.get("company.website", default="https://example.com")
```

## Советы

1. **Начните с примера** - скопируйте `config.json` и адаптируйте под свой бизнес
2. **Тестируйте изменения** - после изменения config.json перезапустите бота
3. **Храните резервные копии** - создавайте `config.production.json` и `config.development.json`
4. **Не коммитьте секреты** - добавьте `config.json` в `.gitignore` если он содержит чувствительные данные
5. **Используйте шаблон** - создайте `config.example.json` с placeholder значениями для других разработчиков
